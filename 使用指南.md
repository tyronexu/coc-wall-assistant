# CoC 刷墙助手 · 使用指南

> 本指南面向最终用户，详细介绍如何使用本工具。所有功能均不连接游戏服务器，数据仅存于本地浏览器。

---

## 一、项目简介

**CoC 刷墙助手**是一个纯本地、零依赖、合规的部落冲突（Clash of Clans）辅助工具，以 PWA 形式运行，可添加到手机桌面像原生 APP 一样使用。

### 核心功能

| 模块 | 功能 |
| --- | --- |
| 资源规划 | 计算城墙升级所需金币总量、完成天数、预计完成日期 |
| 策略速查 | 查阅打资源兵种搭配、时间窗口、资源管理等策略 |
| 训练提醒 | 手动录入兵种训练时间，到点本地通知 |
| 进度追踪 | 每日记录资源获取与墙升级数，生成发育曲线 |

### 合规边界

本工具 **不会**：
- 连接 Clash of Clans 游戏服务器
- 模拟点击或自动操作游戏
- 读取游戏内存或拦截网络包

所有数据由用户手动录入，仅做本地数学计算与计时提醒。使用自动化外挂违反 Supercell 服务条款，会导致永久封号。

---

## 二、访问与安装

### 2.1 在线访问

直接在浏览器打开：

```
https://tyronexu.github.io/coc-wall-assistant/
```

### 2.2 安装到手机桌面（推荐）

PWA 支持"添加到主屏幕"，安装后可离线使用、全屏运行、无浏览器地址栏。

**Android（Chrome / Edge）：**
1. 打开上述网址
2. 点击浏览器右上角菜单（⋮）
3. 选择「添加到主屏幕」或「安装应用」
4. 确认后桌面会出现「CoC墙助手」图标

**iOS（Safari）：**
1. 用 Safari 打开上述网址
2. 点击底部分享按钮（方框+向上箭头）
3. 选择「添加到主屏幕」
4. 点击「添加」

### 2.3 本地运行（开发者）

本项目为纯静态站点，无需 Node.js 或构建工具。

```bash
# 在项目根目录执行
python -m http.server 8080
# 浏览器打开 http://localhost:8080
```

> ⚠️ 不要直接双击 `index.html` 用 `file://` 打开，Service Worker、IndexedDB、通知功能会因协议限制失效。

---

## 三、功能详解

### 3.1 资源规划（首页）

用于计算把一批城墙从当前等级升到目标等级所需的资源和时间。

**操作步骤：**

1. 选择**大本营等级**：自动提示该大本营支持的墙等级上限（仅参考）
2. 选择**当前墙等级**：你现在城墙的等级（0-15）
3. 选择**目标墙等级**：想升到的等级
4. 输入**需要升级的墙数量**：要刷的墙的块数
5. 输入**每日可刷金币**：你每天平均能获取的金币量
6. 点击「**计算**」

**输出结果：**

| 项目 | 说明 |
| --- | --- |
| 单块升级成本 | 一块墙从当前等级升到目标等级的总金币 |
| 总成本 | 单块成本 × 墙数量 |
| 预计完成天数 | 总成本 ÷ 每日金币（向上取整） |
| 预计完成日期 | 今天 + 完成天数 |
| 升级成本明细表 | 逐级列出 Lv N → Lv N+1 的金币和累计成本 |

**示例：** 从 Lv5 升到 Lv9，100 块墙，每天刷 4M 金币 → 总成本约 6 亿金币，约 15 天完成。

---

### 3.2 策略速查

内置的打资源知识库，直接翻阅即可。包含以下分类：

- **兵种搭配**：胖法流、电龙流、矿工流、夜世界野蛮人海等
- **匹配值策略**：升本节奏、夜世界双倍刷墙等
- **时间窗口**：黄金 Farm 时段、清晨资源库等
- **资源管理**：保护圣水、部落城堡存仓、连胜奖励
- **合规提醒**：明确列出会导致封号的行为

> 策略内容为公开游戏知识整理，仅供参考，以游戏内实际机制为准。

---

### 3.3 训练完成提醒

手动录入兵种和数量，训练完成时通过浏览器通知提醒你回来收兵。

**操作步骤：**

1. 首次使用点击「**申请通知权限**」并允许
2. 从下拉菜单选择**预设兵种**（野蛮人、巨人、飞龙、法术、英雄等）
3. 输入**数量**（或直接在「自定义总秒数」填入总时间）
4. 可选：填写**备注**（如"第一波胖法"）
5. 点击「**添加提醒**」

**已设提醒列表**会显示每个提醒的：
- 兵种名称与数量
- 预计完成时间
- 剩余时间
- 备注

点击「删除」可取消该提醒。

**注意事项：**
- 提醒功能依赖浏览器通知，请确保浏览器通知权限已开启
- PWA 后台定时不可靠，**需要 APP 在前台运行**才能准时触发
- 页面刷新后，未触发的提醒会自动恢复调度

---

### 3.4 进度追踪可视化

每日记录资源获取量和墙升级数，生成累计发育曲线，直观展示长期刷墙进度。

**操作步骤：**

1. 选择**日期**（默认今天）
2. 输入**今日金币获取**数量
3. 输入**今日圣水获取**数量
4. 输入**今日完成的墙升级数**（块）
5. 点击「**保存记录**」

**发育曲线**会自动生成三张折线图：

| 图表 | 内容 |
| --- | --- |
| 累计金币获取 | 按日期累计的金币总量 |
| 累计圣水获取 | 按日期累计的圣水总量 |
| 累计完成墙数 | 按日期累计升级的墙块数 |

**历史记录**列表显示所有已保存的记录，可删除单条。

> 同一天多次保存会**累加**到当天记录，不会覆盖。如需修改某天数据，先删除再重新录入。

---

## 四、数据存储与隐私

- 所有数据（提醒任务、每日进度）存储在浏览器的 **IndexedDB** 中，**不上传任何服务器**
- 清除浏览器数据或卸载 PWA 会导致数据丢失，目前暂不支持云端同步
- 城墙成本、兵种训练时间等内置数据存于 [`app/data.js`](./app/data.js)，可手动编辑调整

---

## 五、主题切换

点击页面右上角的 🌙 / ☀️ 图标可切换**深色 / 浅色**主题，设置会保存在浏览器本地。

---

## 六、常见问题

**Q：为什么直接打开 index.html 没反应？**
A：PWA 必须通过 http/https 访问，`file://` 协议下 Service Worker 等功能会被浏览器禁用。请用本地服务器或部署到 GitHub Pages。

**Q：训练提醒到点没通知？**
A：请检查：① 浏览器通知权限是否开启；② APP 是否在前台运行（后台会被系统挂起）；③ 设备是否开启了勿扰模式。

**Q：换手机后数据还在吗？**
A：数据存在原手机的浏览器本地存储中，换手机不会自动同步。需要手动记录后重新录入。

**Q：可以自动读取游戏里的资源数量吗？**
A：不可以。本工具不连接游戏，所有数据必须手动输入。自动读取游戏数据属于违规行为，会封号。

**Q：城墙成本数据不准怎么办？**
A：编辑 [`app/data.js`](./app/data.js) 中的 `WALL_COST` 对象，修改对应等级的金币数值后刷新页面即可。

---

## 七、技术架构

| 组件 | 说明 |
| --- | --- |
| `index.html` | 单页入口，4 个 Tab 导航 |
| `manifest.webmanifest` | PWA 安装配置 |
| `sw.js` | Service Worker，离线缓存 + 通知触发 |
| `styles.css` | 深浅主题与组件样式 |
| `app/main.js` | SW 注册、主题切换、Tab 路由 |
| `app/db.js` | IndexedDB 封装（tracker / reminders 两个存储） |
| `app/data.js` | 城墙成本、兵种时间、策略知识库（内置数据） |
| `app/notify.js` | 通知权限申请与定时器调度 |
| `app/chart.js` | 纯 SVG 折线图，无第三方依赖 |
| `app/planner.js` | 资源规划计算器 |
| `app/guide.js` | 策略速查 |
| `app/reminder.js` | 训练提醒 |
| `app/tracker.js` | 进度追踪 |

---

## 八、合规声明

本工具为合规辅助工具，严格遵守 Supercell 服务条款。使用者应当：
- 手动操作游戏，自行安排在线时段
- 不使用任何宏、自动点击器、脚本等自动化工具
- 不共享账号或使用代练服务

违规使用游戏外挂将导致账号被永久封禁，且通常不可申诉。
